# Roadmap

- Lexer + parser for a Smalltalk core subset
- Method dictionaries, classes, and message dispatch
- Basic image of core classes (Object, Integer, String)
- Block evaluation and non-local returns
- Smalltalk standard library stubs and tests
